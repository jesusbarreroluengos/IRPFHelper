# Perfil local: usa los valores por defecto de application.properties (BD local)
# No necesita sobreescribir nada; su presencia hace que Spring Boot lo reconozca
# cuando el deploy arranca Tomcat con -Dspring.profiles.active=local
